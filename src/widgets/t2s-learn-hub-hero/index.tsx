import HeroSection from "@/widgets/t2s-shared/hero-section";
import type { Props } from "./schema";

const T2sLearnHubHero = (props: Props) => <HeroSection {...props} />;

export default T2sLearnHubHero;
